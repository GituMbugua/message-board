<li>
  <div class="row">
    <div class="well well-default">
      <div class="well-body">
        {{#link-to 'post' post.id}}<h3>{{post.title}}</h3>{{/link-to}}
        <h4>~{{post.author}}</h4>
      </div>
    </div>
  </div>
</li>
