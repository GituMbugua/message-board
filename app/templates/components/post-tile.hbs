<li>
  <div class="well">
    {{#link-to 'post' post.id}}<h3 data-toggle="tooltip" title="Open to see more">{{post.title}}</h3>{{/link-to}}
    {{totalAnswers}} <h3 class="special-icon">{{special-icon post}}</h3>
    <h4>~{{post.author}}</h4>
  </div>
</li>
